# OTPilot Environment Variables
#
# OTPilot uses a credentials.json file provided during `otpilot setup`.
# No environment variables are required for normal operation.
#
# The credentials file is stored at: ~/.otpilot/credentials.json
# The OAuth token is stored at:      ~/.otpilot/token.json
#
# For setup instructions, see docs/SETUP.md
